pipeline {
    // Définition de l'agent sur lequel le pipeline sera exécuté
    agent any

    // Définition des différentes étapes (stages) du pipeline
    stages {
        // Étape de test / greeting
        stage('Greeting') {
            steps {
                echo 'Hello Datascientest'
            }
        }
    }
}
